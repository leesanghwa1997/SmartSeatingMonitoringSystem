# Smart Seating Monitoring System - Backend

이 디렉토리는 의자 센서 데이터를 실시간으로 중계하기 위한 Node.js/Express 및 WebSocket 서버를 포함합니다.

## 1. 사전 요구 사항
* Node.js (v14 이상 권장)
* npm (Node.js 설치 시 포함됨)

## 2. 설치 방법
`backend` 디렉토리로 이동하여 의존성 패키지를 설치합니다.

```bash
cd backend
npm install
```

## 3. 서버 실행 방법

### 개발 모드 (추천)
코드가 변경되면 자동으로 서버가 재시작됩니다.
```bash
npm run dev
```

### 일반 실행
```bash
npm start
```
* 기본 포트는 **8080**입니다. (`http://localhost:8080`, `ws://localhost:8080`)

## 4. WebSocket 연결 확인 방법

### 방법 1: 브라우저 콘솔 활용
1. 서버를 실행합니다.
2. 크롬 등 웹 브라우저에서 개발자 도구(F12)를 엽니다.
3. `Console` 탭에 다음 코드를 입력하여 실행합니다.

```javascript
const ws = new WebSocket('ws://localhost:8080');

ws.onopen = () => {
    console.log('✅ 서버에 연결되었습니다!');
    ws.send('Hello Server!');
};

ws.onmessage = (event) => {
    console.log('📩 메시지 수신:', event.data);
};

ws.onclose = () => {
    console.log('❌ 연결이 종료되었습니다.');
};
```
4. 콘솔에 "✅ 서버에 연결되었습니다!"가 뜨면 성공입니다.

### 방법 2: 테스트 스크립트 실행
`test_client.js` 파일이 있다면 다음 명령어로 테스트할 수 있습니다.
```bash
node test_client.js
```
